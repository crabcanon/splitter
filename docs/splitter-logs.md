# Splitter Implementation Logs

| Time | Level | Area | Problem | Resolution | Preventive Note |
|---|---|---|---|---|---|
| 2026-06-30 12:49 +08:00 | INFO | Bootstrap | Initial repository contains README design notes only; no FastAPI app or package layout exists yet. | Proceeding with scoped scaffold based on README requirements. | Start future work by checking `splitter-tasks.md` before editing. |
| 2026-06-30 13:05 +08:00 | WARN | Test | `pytest` reported a Starlette/FastAPI `TestClient` deprecation warning for `httpx`. | Tests still passed; keep an eye on FastAPI/Starlette test client changes in future upgrades. | Do not treat this warning as splitter logic failure; update test client dependency if it becomes an error. |
| 2026-06-30 13:05 +08:00 | WARN | Tooling | `uv` fell back from hardlinks to file copies on Windows, and `.pytest_cache/` was not readable by `git status`. | Added `.pytest_cache/` to `.gitignore`; no runtime impact. | Keep generated caches ignored and avoid depending on cache directory access. |
| 2026-06-30 13:07 +08:00 | WARN | Tooling | Recursive `Get-ChildItem -Recurse` still tried to descend into `.pytest_cache/` and hit permission denied. | Stopped using recursive raw directory listing for verification; use `rg --files` or explicit excludes for generated caches. | Prefer `rg --files` for repository inventory, consistent with project workflow. |
| 2026-06-30 13:20 +08:00 | ERROR | Refactor | After flattening `src/splitter` to `src`, tests failed with `ModuleNotFoundError: No module named 'splitter'`. | Updating imports, package metadata, Docker entrypoint, tests, and docs to use `src` as the source root. | After directory refactors, run `rg "splitter\\."` and `uv run --extra dev pytest` before continuing. |
| 2026-06-30 13:25 +08:00 | WARN | Entrypoint | Root `main.py` shadowed `src/main.py` during `python -c "from main import app"` and caused a circular import. | Converted root `main.py` into an explicit importlib wrapper around `src/main.py`. | Keep `uvicorn main:app --app-dir src` as the primary local server command; test root wrappers separately. |
| 2026-06-30 14:25 +08:00 | INFO | Entrypoint | Root `main.py` still used an importlib-based wrapper because it had the same module name as `src/main.py`. | Renamed the application module to `src/app.py`; root `main.py` is now a small local runner that imports `app` cleanly after adding `src` to `sys.path`. | Avoid giving root scripts and source modules the same import name. |
| 2026-06-30 13:30 +08:00 | INFO | Verification | First hidden uvicorn smoke check waited only 3 seconds and failed to connect. | Foreground startup showed no crash; reran with an 8 second wait and `/healthz` returned ok. | For uv/uvicorn smoke tests on Windows, allow a longer startup window before probing. |
| 2026-06-30 13:34 +08:00 | WARN | Tooling | `uv build` failed when initializing the global uv cache under the user profile because a cache path already existed in an unexpected state. | Use a workspace-local `UV_CACHE_DIR=.uv-cache` for build verification and keep `.uv-cache/` ignored. | Prefer workspace-local tool caches when global cache state is unreliable. |
| 2026-06-30 13:45 +08:00 | INFO | Strategies | Existing strategy layer still contained native fallback implementations for Markdown, JSON, token, hierarchy, etc. | Replacing the fallback classes with one framework adapter layer backed by LlamaIndex/LangChain/Chonkie parsers. | Do not reimplement parser algorithms already provided by the underlying frameworks; only adapt configuration and output DTOs. |
| 2026-06-30 13:50 +08:00 | WARN | Dependencies | `llama-index-node-parser-chonkie>=0.3,<0.5` was unsatisfiable; the available package line is `0.1.x`. | Changed the dependency constraint to `llama-index-node-parser-chonkie>=0.1,<0.2`. | Check actual package version availability before aligning all LlamaIndex extension packages to core version ranges. |
| 2026-06-30 14:15 +08:00 | WARN | Dependencies | Installing Chonkie as a default dependency pulled in a heavy torch/transformers/CUDA chain and exceeded the verification timeout. | Moved `llama-index-node-parser-chonkie` to the optional `chunker` extra while keeping the `Chunker` strategy as a direct framework adapter. | Keep heavyweight parser integrations optional when they are not needed for the common deployment path. |
| 2026-06-30 15:05 +08:00 | INFO | API | `/split` contract needed to be simplified and old file/URL source loading paths would keep the API larger than necessary. | Removing source loader support, URL safety settings, request-body provider overrides, and stale docs examples; `/split` now accepts only `content` and `mode`. | When changing API shape, update schemas, orchestrator, OpenAPI, README, deployment docs, tests, and smoke checks together. |
| 2026-06-30 15:30 +08:00 | INFO | API | Split response still exposed diagnostics and Swagger only had a couple of request examples. | Removed diagnostics from `SplitResponse` and added one runtime Swagger example for each of the 12 strategies. | Verify `/openapi.json` after schema changes so Swagger examples and response fields match the running app. |
| 2026-06-30 15:50 +08:00 | ERROR | Dependencies | `CodeSplitter` first lacked `tree_sitter_language_pack`, then the current language-pack parser expected `str` while LlamaIndex passed `bytes`. | Added `tree-sitter-language-pack` as a default runtime dependency, passed an adapter parser into LlamaIndex `CodeSplitter`, and covered the Swagger sample with a regression test. | Framework adapters can rely on optional packages and subtle parser API versions; run each published Swagger sample at least once before treating it as ready. |
| 2026-06-30 16:55 +08:00 | WARN | Strategies | Natural-language splitters used framework defaults that kept medium Chinese prose as one node; `Chunker` also needed its LlamaIndex extension installed by default. | Set smaller default chunk windows, added a Chinese/English sentence splitter for windowed strategies, made explicit Semantic use MockEmbedding when no provider is configured, and moved the Chonkie LlamaIndex extension into default dependencies. | For API-only mode without per-request params, choose sensible service defaults and test representative Chinese prose plus every Swagger sample. |
| 2026-06-30 18:05 +08:00 | ERROR | Deployment | Docker image reached about 12.8G because `llama-index-node-parser-chonkie` pulled `torch`, `triton`, `transformers`, `sentence-transformers`, and Linux `nvidia-*` CUDA wheels. | Removing the Chonkie LlamaIndex extension from default dependencies, using a direct `chonkie.RecursiveChunker` adapter, trimming full `llama-index`/`langchain` meta packages, dropping file-reader dependencies now that `/split` accepts content only, and switching Docker to a cleaned multi-stage runtime image. | Before adding parser integrations, inspect their extras/transitive dependencies with `uv lock`/`uv tree`; keep neural/GPU/file-reader integrations optional unless the API truly requires them. |
| 2026-06-30 18:35 +08:00 | INFO | Deployment | Optimized image needed proof against both dependency regression and runtime breakage. | Final `docker images splitter-api:slim` reports 483MB, `docker image inspect` reports 111282704 bytes of content, `/app/.venv` is 220M, local tests passed, and all 12 Swagger examples passed inside the Linux container. | Keep Docker size checks and a container smoke test in the release checklist after dependency changes. |
| 2026-06-30 18:45 +08:00 | WARN | Deployment | Full container verification showed `HTMLNodeParser` still needs `bs4`; local tests had passed because the old virtualenv retained removed reader dependencies. | Added `beautifulsoup4` as a small direct runtime dependency instead of restoring the heavy `llama-index-readers-file` package. | After dependency slimming, rebuild the image from scratch and execute all published Swagger examples inside the container, not only in the local venv. |
